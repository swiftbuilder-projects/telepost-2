# Telepost 2

This is a **Next.js application** generated by SwiftBuilder.
It is completely self-contained and ready to deploy.

## Project Structure

- `src/app/page.tsx`: The main entry point.
- `src/components/sacco-2`: The template source code.
- `src/data/content.ts`: The content data (images, text) for your site.
- `public/assets`: Project strings and images.
- `public/templates`: Template assets.

## Local Development

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run the development server:
   ```bash
   npm run dev
   ```

3. Open [http://localhost:3000](http://localhost:3000).

## 🚀 Deploying to Coolify

This project is optimized for Coolify.

1. **Push to GitHub**: Ensure this repository is pushed to your GitHub account.
2. **Create Resource**: In Coolify, click "+ New Resource".
3. **Select Source**: Choose "Git Repository" (GitHub).
4. **Select Repository**: Pick this repository.
5. **Configuration**: 
   - Build Pack: **Nixpacks** (Recommended) or Next.js
   - Ports: 3000
   - Install Command: `npm install`
   - Build Command: `npm run build`
   - Start Command: `npm run start`
6. **Deploy**: Click "Deploy".

## Other Hosting (Vercel/Netlify)

This is a standard Next.js app. You can verify deploy it to Vercel or Netlify by simply connecting your GitHub repository.
